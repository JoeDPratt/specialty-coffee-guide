import { cn } from "@/utils/classes/merge";

export default function SearchInput({ className }: { className: string }) {
    return (
        <div className={cn(
            "flex items-center",
            className)}>

        </div>
    )
}